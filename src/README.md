This folder is intentionally empty at the start.

Recommended implementation order:
1. `src/models/`
2. `src/scoring/`
3. `src/logging/`
4. `src/platforms/`
5. `src/apply/`

Do not start with fragile browser automation before data models and logs exist.
